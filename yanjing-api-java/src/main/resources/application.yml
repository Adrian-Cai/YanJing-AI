server:
  port: ${API_PORT:8000}

spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://root:Caijinwei%262026@localhost:27017/yanjing?authSource=admin}
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      username: ${REDIS_USERNAME:}
      password: ${REDIS_PASSWORD:Caijinwei&2026}

management:
  endpoints:
    web:
      exposure:
        include: health,info
